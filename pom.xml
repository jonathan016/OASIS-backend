<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.oasis</groupId>
    <artifactId>oasis</artifactId>
    <version>${oasis.version}</version>
    <modules>
        <module>model</module>
        <module>repository</module>
        <module>service</module>
        <module>web-controller</module>
        <module>web-model</module>
    </modules>
    <packaging>pom</packaging>

    <name>OASIS</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.7</maven.compiler.source>
        <maven.compiler.target>1.7</maven.compiler.target>

        <oasis.version>1.1.0-SNAPSHOT</oasis.version>
        <springwebmvc.version>5.1.0.RELEASE</springwebmvc.version>
        <springcontext.version>5.0.8.RELEASE</springcontext.version>
        <springsecurity.version>3.1.0.RELEASE</springsecurity.version>
        <mongodb.version>2.1.0.RELEASE</mongodb.version>
        <jackson.version>2.9.4</jackson.version>
        <lombok.version>1.18.2</lombok.version>
        <orika.version>1.5.2</orika.version>
        <model.version>${oasis.version}</model.version>
        <repository.version>${oasis.version}</repository.version>
        <service.version>${oasis.version}</service.version>
        <web-controller.version>${oasis.version}</web-controller.version>
        <web-model.version>${oasis.version}</web-model.version>
    </properties>

    <repositories>
        <repository>
            <id>spring-libs-release</id>
            <name>Spring Releases</name>
            <url>https://repo.spring.io/libs-release</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <!--External Dependency-->
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-webmvc</artifactId>
                <version>${springwebmvc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-context</artifactId>
                <version>${springcontext.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-crypto</artifactId>
                <version>${springsecurity.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.data</groupId>
                <artifactId>spring-data-mongodb</artifactId>
                <version>${mongodb.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>ma.glasnost.orika</groupId>
                <artifactId>orika-core</artifactId>
                <version>${orika.version}</version>
            </dependency>

            <!--Module Dependency-->
            <dependency>
                <groupId>com.oasis</groupId>
                <artifactId>model</artifactId>
                <version>${model.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oasis</groupId>
                <artifactId>repository</artifactId>
                <version>${repository.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oasis</groupId>
                <artifactId>service</artifactId>
                <version>${oasis.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oasis</groupId>
                <artifactId>web-controller</artifactId>
                <version>${oasis.version}</version>
            </dependency>
            <dependency>
                <groupId>com.oasis</groupId>
                <artifactId>web-model</artifactId>
                <version>${oasis.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <finalName>OASIS</finalName>
    </build>
</project>
